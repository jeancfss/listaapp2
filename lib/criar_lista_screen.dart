import 'package:flutter/material.dart';

class CriarListaScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Criar Nova Lista'),
      ),
      body: Center(
        child: Text('Criar Lista'),
      ),
    );
  }
}