import 'package:flutter/material.dart';

class DetalhesListaScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Lista de COmpras'),
      ),
      body: Center(
        child: Text('Lista de compras Supermercado'),
      ),
    );
  }
}